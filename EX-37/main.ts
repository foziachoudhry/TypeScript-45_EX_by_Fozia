//Function
function make_shirt (size: string = "Large",message: string = "I Love Typescript"){
console.log (`Make a ${size} shirt with the ${message} print on it`)
}

//Calling a Function with by default,
make_shirt();
 // Calling a function with medium size default message,
 make_shirt("medium")

 //Calling a function with small siz also different message,

 make_shirt("small","I Like Coding")
